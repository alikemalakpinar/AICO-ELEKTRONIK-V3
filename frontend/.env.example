# ===========================================
# AICO Elektronik Frontend Environment Variables
# Copy this file to .env.local and fill in the values
# ===========================================
#
# ARCHITECTURE NOTE:
# This app uses a same-origin proxy architecture for API calls:
# - Client-side API calls use relative URLs (/api/...)
# - nginx proxies /api/ requests to the FastAPI backend
# - No NEXT_PUBLIC_BACKEND_URL needed - same image works across all environments
#
# ===========================================

# ===== Build-time variables (NEXT_PUBLIC_*) =====
# These are inlined at build time by Next.js
# NOTE: API calls do NOT use NEXT_PUBLIC_* vars - they use same-origin proxy

# Site URL for metadata/SEO only
NEXT_PUBLIC_SITE_URL=https://aicoelektronik.com

# Allowed image domains for next/image (comma-separated)
# Example: cdn.aicoelektronik.com,images.unsplash.com
NEXT_PUBLIC_IMAGE_DOMAINS=

# ===== Runtime variables (server-side only) =====
# These can be changed at container runtime without rebuilding

# Internal API URL for server components (Docker internal network)
# This is used only for SSR/SSG, not for client-side API calls
# In Docker: http://backend:8001
# Local dev: http://localhost:8001
INTERNAL_API_URL=http://localhost:8001

# ===== Development Settings =====

# Skip telemetry
NEXT_TELEMETRY_DISABLED=1
