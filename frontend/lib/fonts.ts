// Font configuration
// Using system font stack - fonts are defined in tailwind.config.js
// In production with network access, you can switch to next/font/google

export const fontVariables = '';

// Note: Fonts are loaded via Tailwind CSS configuration:
// - Inter (sans-serif) for headings and body
// - JetBrains Mono (monospace) for technical data
//
// To enable Google Fonts (requires network access during build):
// import { Inter, JetBrains_Mono } from 'next/font/google';
// export const inter = Inter({ subsets: ['latin'], display: 'swap', variable: '--font-inter' });
// export const jetbrainsMono = JetBrains_Mono({ subsets: ['latin'], display: 'swap', variable: '--font-mono' });
// export const fontVariables = `${inter.variable} ${jetbrainsMono.variable}`;
